# Compile

$ zig build -Dgnunet-prefix=/path/to/your/gnunet -Dadditional-include-paths=/path/to/your/include -Dadditional-lib-paths=/path/to/your/libs

# Run

$ ./zig-out/bin/gnunet-identity
$ ./zig-out/bin/gnunet-service-zig (ctrl-c to exit)
