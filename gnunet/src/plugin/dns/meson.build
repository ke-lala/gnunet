shared_module(
    'gnunet_plugin_block_dns',
    ['plugin_block_dns.c'],
    dependencies: [libgnunetutil_dep, libgnunetblockgroup_dep],
    include_directories: [incdir, configuration_inc],
    install: true,
    install_dir: get_option('libdir') / 'gnunet',
)
